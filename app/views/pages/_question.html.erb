<tr>
  <td>
    <%= "#{question.num}. #{question.content}" %>
  </td>
  <td>
    <ul>
      <% question.answers.sort.each do |answer| %>
        <% if answer.verified %>
          <%= render :partial => 'answer', :locals => { :question => question,
                                                        :answer => answer, :f => f } %>
        <% end %>
      <% end %>

      <% if question.extra_answer %>
        <%= render :partial => 'extra_answer', :locals => { :question => question, :f => f } %>
      <% end %>
    </ul>
  </td>
</tr>
